<?php
    $title = $entity->getTitle($this->translate('on'));
    $this->headTitle($title);
    $this->layout()->title = $title;
?>

<p>Here comes a standard text that goes for all non-certified carriers.</p>